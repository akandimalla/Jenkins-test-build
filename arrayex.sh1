#!/bin/bash
##simple array list and loop for display

SERVERLIST=("webserver01" "appserver01" "relayserver01" "FWserver01")
COUNT=0

for INDEX in "${SERVERLIST[@]}"; do
 	echo "Processing Server: ${SERVERLIST[COUNT]}"
	COUNT="`expr $COUNT + 1`"
done

